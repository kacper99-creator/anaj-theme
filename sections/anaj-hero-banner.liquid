<div class="test-anaj-hero-banner">
  <h2>{{ section.settings.title }}</h2>
  <p>{{ section.settings.subtitle }}</p>
</div>

{% schema %}
{
  "name": "TEST – ANAJ hero banner",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Tytuł",
      "default": "Testowy banner ANAJ"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Podtytuł",
      "default": "Jeśli to widzisz, sekcja działa."
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name": "TEST – ANAJ hero banner"
    }
  ]
}
{% endschema %}
